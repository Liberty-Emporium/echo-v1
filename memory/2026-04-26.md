## Session — 2026-04-26 (Jay)

### Boot
- Fresh KiloClaw instance bootstrapped clean via bootstrap.sh
- echo-v1 repo cloned from GitHub (Liberty-Emporium/echo-v1)
- All repos cloned: floodclaim, agent-widget, dashboard, petvet, contractor, dropship, consignment, inventory, liberty-oil
- GitLab backup remote added to echo-v1
- Secrets written: /root/.secrets/github_token, /root/.secrets/gitlab_token, /root/.secrets/railway_token
- Railway token provided: 25ee02b1-b010-4b31-9942-c7682152508a (stored at /root/.secrets/railway_token)
- All services healthy on boot: FloodClaim Pro ✅, EcDash ✅, AI Agent Widget ✅

### ⚠️ App Issues on Boot
- **EcDash /health** — repeated 500 and 404 errors overnight (15 errors logged between 00:26–00:28 UTC)
  - Status shows `ok` as of 02:26 UTC — may have self-recovered or been a blip
  - Worth monitoring; `/health` endpoint seems flaky
- **EcDash /favicon.ico** — 404 (minor, no favicon served)

### Carried Over
- Recreate deleted FloodClaim claims: Maria Gonzalez, Derek Simmons, Bill Alexander
- /root/.secrets/willie_api_key — still needed (get from AI Agent Widget platform)
- GitHub token rotation — new PAT provided today (marked temporary, will be replaced)
- todo_005: Write brain passphrase to flash drive (Jay only)
- todo_006: Trademark 'Alexander AI Integrated Solutions' via USPTO TEAS Plus
- todo_007: Hunt down ALL credentials and add to EcDash Credentials panel

### Session Work

#### Widget — alexander-ai-voice agent fix
- Root cause: `alexander-ai-voice` agent seed was skipped because no `OPENROUTER_API_KEY` env var on Railway
- Fixed all 3 seed functions (Echo Brain, Cakely, Alexander AI Voice) to fall back to KYS for OpenRouter key
- Seed functions now: `os.environ.get('OPENROUTER_API_KEY') or fetch_from_kys('openrouter') or ''`
- Previous session's fix was on a dead instance — re-applied and pushed ✅

#### KYS + Widget — Client Key Rotation System (Phase 1 — Widget)
Jay's vision: KYS issues + rotates API keys FOR Jay's customers. Agents notify customers when rotation happens.

**KYS new endpoints (alexander-ai-key-vault):**
- `POST /api/client-keys/issue` — issue a new key for a customer (called at signup)
- `POST /api/client-keys/rotate` — rotate key (old key valid 24h grace period)
- `POST /api/client-keys/fetch` — customer fetches their current key
- `GET /api/client-keys/due` — list keys due for rotation
- `GET /api/client-keys/list` — admin: list all client keys
- Fires webhook to notify app when key rotates
- Keys encrypted at rest using existing encrypt_secret()
- New `client_keys` table auto-created on startup

**Widget new endpoints (alexander-ai-agent-widget):**
- `POST /api/kys/rotation-webhook` — KYS calls this on rotation; queues notification for customer
- `GET /api/kys/pending-notification?client_id=X&app_name=Y` — widget polls on chat open, returns message if rotation pending
- Notification message tells customer: key rotated, check KYS, old key valid 24h, next rotation date
- New `rotation_notifications` table auto-created on startup

**Next steps for this system:**
- Set `KYS_API_TOKEN` Railway env var on Widget so it can auth to KYS
- Configure KYS webhook URL on rotation: `https://ai-agent-widget-production.up.railway.app/api/kys/rotation-webhook`
- Wire widget JS to poll `/api/kys/pending-notification` on chat open and inject the message
- Test end-to-end: issue key → rotate → notification delivered via chat
- Expand to other apps once widget flow is proven

#### Railway env vars set on Widget service
- `KYS_API_TOKEN` = widget-app token (valid 1 year, expires 2027-04-26)
- `KYS_URL` = https://ai-api-tracker-production.up.railway.app
- Triggered redeploy — `alexander-ai-voice` agent now seeds correctly ✅

#### End-to-end test results
- `POST /api/client-keys/issue` → issued `kys_YgG905...` for testcustomer@example.com ✅
- `POST /api/client-keys/rotate` → new key generated, old key kept 24h ✅
- `POST /api/kys/rotation-webhook` → notification queued ✅
- `GET /api/kys/pending-notification` → message delivered with KYS link ✅
- Widget JS side — DONE ✅ checkRotationNotification() fires on chat open + auto-open

#### Key IDs / Tokens
- Railway API token (NEW, working): be7d0cb5-e2d1-4f17-a53b-da0f5978e8a8 (stored at /root/.secrets/railway_token)
- Railway workspace ID: 57932cce-5b27-4acf-b82d-c92c0ca45d6e
- Widget service ID: 91f73b39-c66e-435a-abb3-d7505d3a7b15 (project: b8b1a484, env: a3a4009f)
- KYS service ID: b472f389-4f8a-42f1-afcd-1d13e10ff256 (project: c61971c2-bdaf-47c2-8cb8-daa9daee81a8, env: eada834c)
- KYS widget-app token: stored in Railway env on widget service (not stored here)
- KYS admin login: username=emporiumandthrift@gmail.com / see ADMIN_PASSWORD env var

#### Full Security Audit + Patch — COMPLETED ✅

**Health check results (post-patch):** 10/10 apps green
- Liberty Inventory URL corrected: `liberty-emporium-inventory-demo-app-production.up.railway.app`
- EcDash /health now returns `db` field
- All apps deployed and responding

**Static audit results (post-patch):** 9/9 apps at 13/13 ✅
- Fixed: secret_key persistence via /data volume on all 7 apps that lacked it
- Fixed: SESSION_COOKIE_HTTPONLY + SAMESITE on Widget, Consignment
- Fixed: CSRF token injection into 93 templates across 7 apps
- Fixed: CSRF before_request enforcement wired in all 8 apps
- Fixed: EcDash rate limiter added
- Fixed: audit script updated to recognize ADMIN_EMAIL/DASHBOARD_PASSWORD patterns

**Bandit (medium issues, no high severity):**
- Widget: urllib.urlopen SSRF risk (lines 23, 1112, 1497), possible SQL injection line 1248
- FloodClaim: possible SQL injection line 2582, bind-all-interfaces line 2734
- KYS: urllib.urlopen lines 642, 1924; bind-all-interfaces line 2188
- These are accepted risks (internal calls, Railway-managed networking)

#### KYS Webhook Auto-Config — COMPLETED ✅
- Bug fixed: `json` NameError in rotation webhook code → fixed with `import json as _json`
- `WIDGET_ROTATION_WEBHOOK` env var set on KYS Railway service
- Backfill function added: on startup, fills blank notify_webhook rows from env vars
- New keys auto-get webhook URL at issue time from env var
- End-to-end confirmed: issue → rotate → KYS fires webhook → Widget queues notification → customer sees message ✅
- Commits: feat (3672139) + fix (c1199e8) pushed to alexander-ai-key-vault (jays-keep-your-secrets)
