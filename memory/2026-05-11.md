# Daily Memory — 2026-05-11

## Session Summary
Long productive session with Jay — built out the full Alexander AI support system landing page and fixed several dashboard issues.

---

## Work Done This Session

### 1. Real-time Command Results Fix (socket.js)
- **Problem:** Dashboard sent commands to customer machines but used a blind 3.5s setTimeout poll to get results
- **Fix:** Added `adminSockets` set to `socket.js` — tracks all open admin browser tabs. When `command_result` arrives from customer machine, instantly broadcasts to all admin sockets
- **Fix 2:** Added `adminSocket` to `index.html` — registers one-shot listener instead of polling. Falls back to 15s DB poll as safety net
- **Files:** `src/socket.js`, `public/index.html` in `Alexander-AI-Support-Dashboard`

### 2. Landing Page Built (agents.alexanderai.site)
- Built full sales landing page at `public/landing.html`
- Routes: `/` → landing.html, `/dashboard` → index.html
- **express.static fix:** Added `{ index: false }` to prevent static middleware from serving index.html for `/` before route handler fires
- Cal.com CTAs wired to real events:
  - Discovery Call (15 min): `https://cal.com/leprograms/discovery-call`
  - AI Strategy Session (30 min): `https://cal.com/leprograms/ai-strategy-session`
- Jay's logos from `Liberty-Emporium/Logos` repo embedded
- "Support Dashboard →" link added to nav bar

### 3. Agent Cards — Install Commands
- Added Quick Install footer section to each agent card
- Styled with dark terminal background, cyan/purple glow on hover
- Copy buttons with `copyCode()` JS function (was missing — added it)
- Centered "⚡ QUICK INSTALL" label with ruled lines

### 4. Full Step-by-Step Install Guide (5-year-old proof)
- Big OS picker first: 🪟 Windows · 🍎 Mac · 🐧 Linux
- 3 crystal-clear steps per OS per agent
- Each step has copy button + plain English tip
- "Something went wrong? Book a free call" CTA at bottom of each panel
- Agent Zero: runs natively (Python), Docker only used as sandbox for code execution — customer never touches Docker
- Hermes: 100% native Node.js, no Docker
- Windows Hermes: WSL2 path (wsl --install → Ubuntu → curl)
- Removed confusing "Already installed? Start with one command" leftover text

### 5. Visual Redesign — Eye Candy
- **Font:** Space Grotesk (Google Fonts) — modern, geometric, premium feel
- **Background:** Deeper black `#04080f` with animated ambient glow blobs (cyan left, purple right)
- **Nav:** Glass blur + gradient bottom line + glowing Book button
- **Hero:** Pulsing glow animation behind headline, gradient buttons
- **Cards:** Glassmorphism — `backdrop-filter: blur`, hover lift + cyan glow ring
- **Agent cards:** Cyan theme (Agent Zero) / Purple theme (Hermes)
- **Colored feature check circles** replacing plain ✓

### 6. start-hermes.sh Script
- Pushed to `Hermes-Workspace-Alexander-AI` repo
- One command starts both `hermes gateway run` + `pnpm dev` in background

### 7. MEMORY.md Written + Brain Backed Up
- Full project context written to `/root/.openclaw/workspace/MEMORY.md`
- Pushed to `Liberty-Emporium/echo-v1` on GitHub
- GitLab mirror also updated
- bootstrap.sh ran successfully

---

## Key Facts to Remember

### Dashboard Login
- URL: https://agents.alexanderai.site/dashboard
- Username: `jay`
- Password set via `ADMIN_PASSWORD` env var on Railway (default: `alexander2024`)

### Install Commands (Current State)
**Agent Zero Linux/Mac:** `curl -fsSL https://bash.agent-zero.ai | bash`
**Agent Zero Windows:** `irm https://ps.agent-zero.ai | iex`
**Hermes Linux/Mac:** `curl -fsSL https://raw.githubusercontent.com/Liberty-Emporium/Hermes-Workspace-Alexander-AI/main/install.sh | bash`
**Hermes Windows:** WSL2 required → `wsl --install` → restart → then curl in Ubuntu

### Docker Image Note
`ghcr.io/liberty-emporium/hermes-workspace-alexander-ai:latest` does NOT exist yet — the Docker install command was removed from the landing page. If Jay wants Docker support for Hermes, the image needs to be built and published to GHCR first.

### Jay's Machine Install (2026-05-11)
Jay installed Hermes on his Ubuntu machine (`mingo@mingo-To-Be-Filled-By-O-E-M`):
- Hermes installed at `/home/mingo/hermes-workspace`
- hermes-agent at `/home/mingo/.local/bin/hermes`
- `hermes gateway run` was started
- Needs `cd ~/hermes-workspace && pnpm dev` in a second terminal to launch UI
- Then: http://localhost:3000

---

## Tokens (stored in /root/.secrets/)
- GitHub: `/root/.secrets/github_token` (temporary — Jay will rotate)
- GitLab: `/root/.secrets/gitlab_token` (temporary — Jay will rotate)  
- Railway: `/root/.secrets/railway_token`
- EcDash: `/root/.secrets/ecdash_token`

---

## Open Items / Next Session
- [ ] Jay may need help finishing Hermes setup on his machine (pnpm dev step)
- [ ] Windows native installer for Hermes (PowerShell script) — was discussed but needs building
- [ ] Docker image for Hermes on GHCR — if Jay wants Docker option
- [ ] Test the landing page after latest visual redesign push
- [ ] Rotate GitHub/GitLab tokens after Jay replaces them
- [ ] Real testimonials to replace placeholder ones on landing page
