# Session Notes — 2026-05-02 (Fresh Boot)

## Bootstrap
- New KiloClaw instance on Railway
- GitHub token provided by Jay: saved to /root/.secrets/github_token
- GitLab token provided by Jay: saved to /root/.secrets/gitlab_token
- Ran bootstrap.sh — all tools installed, all repos cloned, remotes configured
- echo-v1 cloned and up to date

## App Health
- EcDash: ✅ HTTP 200 — dashboard is live
- Bridge check skipped — ecdash_token not yet set

## Open Items (carried from 2026-05-01)
- 🔴 Stripe env vars still need to be added in Railway dashboard by Jay
  - STRIPE_SECRET_KEY, STRIPE_PUBLISHABLE_KEY on all apps
  - AI Widget: + price IDs
  - Pet Vet AI: + price ID
- 🟡 EcDash 500 errors — ongoing, likely bot/scanner traffic
- 🟡 Liberty Oil DNS → libertyoilandpropane.com (still pending)

## Secrets Status
- ✅ `/root/.secrets/ecdash_token` — echo-bridge token (Bearer auth on /api/echo-bridge)
- ✅ `/root/.secrets/brain_sync_token` — brain-sync token (X-Brain-Sync-Token header)
  - ⚠️  Needs `BRAIN_SYNC_TOKEN` env var set in Railway → alexander-ai-dashboard service to work
- ❌ `/root/.secrets/willie_api_key` — still missing, get from AI Agent Widget

## Bug Fix Applied
- Agent name reverting to "Echo" was caused by `app_settings.json` being wiped on Railway redeploy
- Fixed: added `echo_agent_name: 'EcDash'` and tagline to `DEFAULT_APP_SETTINGS` in app.py
- Committed and pushed to alexander-ai-dashboard (master branch, ec664ae)

## Crons Set Up
- GitLab backup: daily at 3:00 AM EDT (runs sync-all-to-gitlab.sh, mirrors all repos)

## Rule (carried from 2026-05-01)
- **DO NOT bulk-clone repos** — only clone the repo actively being worked on
- echo-v1 is always present; everything else is on-demand
